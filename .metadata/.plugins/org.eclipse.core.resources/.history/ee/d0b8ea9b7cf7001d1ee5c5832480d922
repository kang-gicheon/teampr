package common;

public class MathUtil {
	public static int discount(int number) {
		return (int)(number*0.95);
	}
	public static int addSum(int[] numbers) {
		int sum = 0;
		for(int i = 0; i < numbers.length; i++) {
			sum += numbers[i];
		}
		return sum;
	}
}
